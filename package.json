{
  "name": "imooc-node",
  "version": "1.0.0",
  "description": "Node Action in iMooc",
  "main": "app.js",
  "scripts": {
    "lint": "eslint .",
    "fix": "eslint --fix .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "pre-commit": [
    "lint",
    "fix"
  ],
  "author": "Erchoc",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.1"
  },
  "devDependencies": {
    "pre-commit": "^1.2.2"
  }
}
