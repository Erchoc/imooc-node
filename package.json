{
  "name": "netfile",
  "version": "1.0.0",
  "description": "A simple Static Resource Web Manage CLI",
  "main": "app.js",
  "bin": {
    "netfile": "bin/netfile"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "dev": "supervisor app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "pre-commit": [
    "lint",
    "lint:fix"
  ],
  "author": "Erchoc",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.1",
    "handlebars": "^4.0.11",
    "yargs": "^12.0.1"
  },
  "devDependencies": {
    "pre-commit": "^1.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Erchoc/netfile.git"
  }
}
